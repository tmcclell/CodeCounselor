Items to review during the demo. Explain each items below and their purpose.

- Instructions
- Prompt Files
- MCP Servers

Have GitHub Copilot build the app and run it in the demo. You could also build it before hand and use it to demonstrate the Coding agent.

- Run App
    - Sample Snippet. These are samples you can put into the Code Counselor. Warning, one of these will set off the Content Understanding Filters, which is a good demonstration.
    
    def get_user_input():
        input = input("Enter your name: ")
        print("Hello " + input + "!")

    get_user_input()

    
    while True:
        print("I will never break this loop.")


- Use GHCP to Create Issues
    “Add a toggle to adjust the humor level of the AI’s responses.”
    “Create a Streamlit frontend instead of HTML/JS.”
    “Write unit tests for the /chat endpoint.”